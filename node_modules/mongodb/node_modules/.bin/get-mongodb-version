#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../get-mongodb-version/bin/get-mongodb-version.js" "$@"
  ret=$?
else 
  node  "$basedir/../get-mongodb-version/bin/get-mongodb-version.js" "$@"
  ret=$?
fi
exit $ret
